services:
  api:
    build: .
    ports:
      - "8001:8001"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./sessions:/app/sessions
      - rag_models:/app/data/models

volumes:
  rag_models:
